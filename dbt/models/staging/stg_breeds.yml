version: 2

models:
  - name: stg_breeds
    description: One row per breed, renamed fields, still unparsed text.
    columns:
      - name: breed_id
        description: Unique breed identifier from the source API.
        tests:
          - unique
          - not_null
      - name: name
        description: Breed name.
        tests:
          - not_null
